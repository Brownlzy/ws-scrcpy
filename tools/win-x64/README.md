Windows x64 runtime tools for portable package builds:

- frpc.exe
- adb.exe
- AdbWinApi.dll
- AdbWinUsbApi.dll
- libusb-1.0.dll

The build script copies them into the portable package as:

- tools/frpc.exe
- tools/adb.exe
